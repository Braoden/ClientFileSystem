{
  "name": "client-file-system",
  "version": "1.1.1",
  "description": "AI-powered client file explorer desktop app",
  "main": "electron/main.js",
  "scripts": {
    "start": "concurrently \"npm run backend\" \"npm run frontend\" \"wait-on http://localhost:3000 && npm run electron\"",
    "backend": "node backend/server.js",
    "frontend": "cd frontend && npm start",
    "electron": "electron .",
    "build": "cd frontend && npm run build",
    "dist": "npm run build && electron-builder"
  },
  "build": {
    "appId": "com.clientfilesystem.app",
    "productName": "Client File System",
    "files": [
      "electron/**/*",
      "backend/**/*",
      "node_modules/**/*"
    ],
    "extraResources": [
      {
        "from": "frontend/build",
        "to": "frontend/build",
        "filter": [
          "**/*"
        ]
      }
    ],
    "win": {
      "target": "nsis",
      "icon": "icons/icon.ico"
    },
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true
    }
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@anthropic-ai/sdk": "^0.39.0",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "multer": "^1.4.5-lts.1"
  },
  "devDependencies": {
    "electron": "^42.4.0",
    "electron-builder": "^26.15.2"
  }
}
